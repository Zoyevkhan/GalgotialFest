package com.example.galgotialfest.util

import android.content.Context
import androidx.work.Data
import androidx.work.Worker
import androidx.work.WorkerParameters
import com.example.galgotialfest.database.FirebaseHandler
import com.example.galgotialfest.model.DetailsUser

/*class DownloadWorker (val context: Context,workParams:WorkerParameters): Worker(context,workParams) {*/

    /*override fun doWork(): Result {
        for(i in 1..1000000){

               Log.d("zoyeb", "doWork: "+i)


        }
        return Result.success()
    }*/
    /*override fun doWork(): Result {
      if( FirebaseHandler().getDataFromFirebaseByGameName())
        var outputData= Data.Builder()
            .putS(KEY_MESSAGE, "This is output message")
            .build()
         return Result.success(listOf<DetailsUser>())
        else
          return  Result.failure()

    }*/


